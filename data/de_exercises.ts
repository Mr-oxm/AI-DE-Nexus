export const tagColors = {
  "FUNDAMENTALS": "#00C9A7",
  "ETL/ELT":      "#F9C80E",
  "SQL":          "#FF6B35",
  "MODELING":     "#845EC2",
  "FILE FORMATS": "#00B4D8",
  "DATABASES":    "#FF6B6B",
  "SPARK":        "#E87040",
  "ORCHESTRATION":"#4CC9F0",
  "STREAMING":    "#F77F00",
  "DATA QUALITY": "#52B788",
  "CLOUD":        "#C77DFF",
  "WHY NOT":      "#F72585",
};
export const exercises = [

  // ─── FUNDAMENTALS ──────────────────────────────────────────────────────────
  {
    id: 1, tag: "FUNDAMENTALS",
    q: "What is a data pipeline and what are its main stages?",
    a: "A data pipeline is an automated sequence of steps that moves data from one or more sources to a destination where it can be used for analysis or decision-making.\n\nMain stages:\n1. INGEST   — Pull raw data from sources (APIs, databases, files, events)\n2. STORE    — Land it somewhere (data lake, object storage like S3)\n3. TRANSFORM — Clean, reshape, enrich the data (ETL/ELT)\n4. SERVE    — Make it available to consumers (BI tools, ML models, apps)\n5. MONITOR  — Track quality, freshness, failures at every step\n\nA simple example: Nightly job pulls orders from MySQL → stores raw JSON in S3 → transforms into clean Parquet → loads into Redshift → analysts query in Tableau."
  },
  {
    id: 2, tag: "FUNDAMENTALS",
    q: "What is the difference between batch processing and stream processing? When would you choose each?",
    a: "BATCH PROCESSING:\n- Processes large chunks of data at scheduled intervals (hourly, daily)\n- Higher latency: results are delayed until the batch runs\n- Simpler to build, debug, and re-run\n- Great for: daily sales reports, monthly invoices, model retraining\n\nSTREAM PROCESSING:\n- Processes data continuously as it arrives, event by event\n- Very low latency: results available in seconds or milliseconds\n- More complex: you must handle late data, ordering, failures\n- Great for: fraud detection, real-time dashboards, live notifications\n\nRule of thumb:\n- If the business can wait hours → batch\n- If the business needs to react NOW → stream\n\nMany real systems use BOTH: stream for real-time alerts, batch for accurate historical reports (Lambda Architecture)."
  },
  {
    id: 3, tag: "FUNDAMENTALS",
    q: "What does idempotency mean in data engineering and why does it matter?",
    a: "Idempotency means running a pipeline multiple times produces the same result as running it once.\n\nWhy it matters:\nPipelines fail. Networks go down. Schedulers re-trigger jobs. If your pipeline is NOT idempotent, re-running it after a failure causes:\n- Duplicate rows in your table\n- Double-counted revenue\n- Corrupted aggregations\n\nHow to make a pipeline idempotent:\n1. DELETE + INSERT: before inserting, delete the records for that time window first\n   DELETE FROM orders WHERE date = '2024-01-15';\n   INSERT INTO orders SELECT * FROM raw WHERE date = '2024-01-15';\n\n2. UPSERT (MERGE): insert if new, update if exists — based on a unique key\n\n3. Overwrite partitions: if using Parquet/Spark, write to a specific partition and overwrite it\n\nBottom line: always ask 'what happens if this runs twice?' before shipping a pipeline."
  },
  {
    id: 4, tag: "FUNDAMENTALS",
    q: "What is data lineage and why do data engineers care about it?",
    a: "Data lineage tracks where data comes from, how it has been transformed, and where it flows to — like a family tree for your data.\n\nExample chain:\nRaw MySQL orders table\n  → S3 raw zone (JSON)\n    → S3 clean zone (Parquet, nulls removed)\n      → Redshift 'orders' table\n        → 'monthly_revenue' dashboard in Tableau\n\nWhy engineers care:\n1. DEBUGGING: when a number looks wrong, lineage tells you exactly which step broke it\n2. IMPACT ANALYSIS: if you change the source schema, you instantly know which 15 downstream tables will break\n3. COMPLIANCE (GDPR etc.): you can prove where personal data lives and delete it across all systems\n4. TRUST: analysts are more confident using data when they can see its full history\n\nTools that track lineage: Apache Atlas, dbt (built-in), OpenMetadata, DataHub."
  },
  {
    id: 5, tag: "FUNDAMENTALS",
    q: "What is the difference between a data lake, a data warehouse, and a data lakehouse?",
    a: "DATA LAKE:\n- Stores raw data in its original format (JSON, CSV, Parquet, images, logs)\n- Schema-on-read: you define structure only when you query it\n- Very cheap storage (S3, GCS, ADLS)\n- Con: can become a 'data swamp' — hard to govern and query\n\nDATA WAREHOUSE:\n- Stores structured, cleaned, transformed data\n- Schema-on-write: structure is defined before loading\n- Fast for SQL analytics (Redshift, BigQuery, Snowflake)\n- Con: expensive, rigid schema, hard to store unstructured data\n\nDATA LAKEHOUSE (modern approach):\n- Combines both: cheap lake storage WITH warehouse-quality reliability\n- Uses open table formats (Delta Lake, Apache Iceberg, Apache Hudi)\n- Supports ACID transactions, schema enforcement, time-travel\n- Examples: Databricks Lakehouse, Snowflake with Iceberg\n\nEvolution: Lake → Warehouse → Lakehouse (best of both worlds)"
  },

  // ─── ETL / ELT ─────────────────────────────────────────────────────────────
  {
    id: 6, tag: "ETL/ELT",
    q: "What is the difference between ETL and ELT?",
    a: "ETL (Extract → Transform → Load):\n- Transform data BEFORE loading it into the destination\n- Transformation happens in a separate processing engine (Spark, Python)\n- Used when destinations have limited compute or you need strict data governance\n- Common in older on-prem data warehouse setups\n\nELT (Extract → Load → Transform):\n- Load raw data FIRST, then transform INSIDE the destination\n- Leverages the massive compute of modern cloud warehouses (BigQuery, Snowflake)\n- Faster to get data in; transformations done with SQL (e.g. dbt)\n- Easier to re-transform: raw data is always there, just re-run the SQL\n\nELT is now the dominant modern approach because:\n- Cloud warehouses are cheap and massively parallel\n- Keeping raw data lets you fix transformation bugs retroactively\n- SQL is accessible to more people than Spark/Python"
  },
  {
    id: 7, tag: "ETL/ELT",
    q: "What is a slowly changing dimension (SCD) and what are the types?",
    a: "A Slowly Changing Dimension (SCD) is a dimension table (like customers or products) where values change slowly over time. The question is: how do you handle historical changes?\n\nSCD Type 1 — OVERWRITE:\n- Just update the record. Old value is lost forever.\n- Use when history doesn't matter (e.g. fixing a typo)\n\nSCD Type 2 — ADD A NEW ROW (most common):\n- Keep the old row, add a new one with effective_date and end_date\n- Full history preserved\n- Example: customer moved from NYC to LA — both rows kept\n  | customer_id | city | start_date | end_date   | is_current |\n  | C001        | NYC  | 2020-01-01 | 2023-06-01 | False      |\n  | C001        | LA   | 2023-06-01 | NULL       | True       |\n\nSCD Type 3 — ADD A COLUMN:\n- Add a 'previous_value' column. Only keeps one level of history.\n- Rarely used — very limited\n\nIn interviews, SCD Type 2 is the answer they're looking for 95% of the time."
  },
  {
    id: 8, tag: "ETL/ELT",
    q: "How would you design an incremental data load vs a full load? When do you use each?",
    a: "FULL LOAD:\n- Truncate the destination table, reload ALL data from source every run\n- Simple to implement, always consistent\n- Con: very slow and expensive as data grows\n- Use when: table is small (< 1M rows), no reliable updated_at column, source data changes randomly\n\nINCREMENTAL LOAD:\n- Only pull rows that are NEW or CHANGED since the last run\n- Much faster and cheaper\n- Requires a watermark: usually a timestamp column (updated_at, created_at)\n\nIncremental pattern:\n  last_run = get_last_run_timestamp()  # stored in a metadata table\n  new_rows = SELECT * FROM source WHERE updated_at > last_run\n  upsert(new_rows, destination)\n  save_run_timestamp(now())\n\nGotchas with incremental:\n- Source must have a reliable updated_at column (not all do)\n- Hard deletes are invisible — you need CDC (Change Data Capture) for those\n- Late-arriving data can be missed if you use strict watermarks"
  },
  {
    id: 9, tag: "ETL/ELT",
    q: "What is Change Data Capture (CDC) and how does it work?",
    a: "CDC is a technique that captures every INSERT, UPDATE, and DELETE that happens in a source database and streams those changes to your pipeline.\n\nWhy it matters:\nNormal incremental loads (using updated_at) miss hard deletes — if a row is deleted in the source, you'd never know. CDC captures it.\n\nHow it works (most common approach — log-based CDC):\n1. Most databases (Postgres, MySQL) write every change to a transaction log (WAL in Postgres, binlog in MySQL)\n2. A CDC tool reads that log and emits change events\n3. Your pipeline consumes those events\n\nPopular CDC tools:\n- Debezium (open source, very popular)\n- AWS DMS\n- Fivetran (managed)\n- Airbyte\n\nCDC event example:\n  { op: 'u', table: 'orders', before: {status:'pending'}, after: {status:'shipped'} }\n\nUsed heavily in real-time pipelines and event-driven architectures."
  },

  // ─── WHY NOT (ETL themed) ───────────────────────────────────────────────────
  {
    id: 10, tag: "WHY NOT",
    q: "Why use ELT instead of ETL in a modern cloud data stack?",
    a: "SHORT ANSWER: Because the warehouse IS the compute engine now.\n\nOld world (ETL made sense):\n- Warehouses were expensive — you couldn't afford to load raw data\n- Transform first in a separate Spark/Python cluster, load only clean data\n- Storage was the bottleneck\n\nNew world (ELT wins):\n- Cloud storage (S3, GCS) is extremely cheap — storing raw data costs almost nothing\n- Cloud warehouses (BigQuery, Snowflake, Redshift) have massive parallel SQL compute\n- Transforming with SQL in the warehouse is faster than spinning up external clusters\n- Raw data is always there: if you find a bug in your transformation, just re-run the SQL\n- Tools like dbt make SQL transformations version-controlled, tested, and documented\n\nWhen ETL still makes sense:\n- Sensitive PII data that must be masked before entering the warehouse\n- Very complex transformations that SQL can't handle (ML feature engineering)\n- Destinations with limited compute (legacy systems)"
  },
  {
    id: 11, tag: "WHY NOT",
    q: "Why use Parquet instead of CSV for storing large datasets?",
    a: "This is one of the most common DE interview questions. Here's the full picture:\n\nCSV problems:\n- Row-based: to read column 'revenue', you read EVERY column on EVERY row\n- No type enforcement: '42' could be a number or a string — no way to tell\n- No compression metadata\n- No schema stored in the file\n- Reading 1TB CSV to get one column = reading 1TB of data\n\nParquet advantages:\n1. COLUMNAR STORAGE: data is stored column by column. To read 'revenue', you only read the 'revenue' column — 10-100x less I/O\n2. COMPRESSION: each column has the same data type, so it compresses extremely well (Snappy, GZIP, ZSTD). Often 5-10x smaller than CSV\n3. SCHEMA EMBEDDED: file contains column names and types — no guessing\n4. PREDICATE PUSHDOWN: query engines skip entire row groups that don't match your WHERE clause — they never even read those bytes\n5. STATISTICS: min/max values stored per column chunk for fast filtering\n\nRule: always use Parquet (or ORC) for analytical data stored in a data lake. Only use CSV for small files, exports to humans, or when a tool requires it."
  },
  {
    id: 12, tag: "WHY NOT",
    q: "Why use a message queue (like Kafka) instead of writing directly to a database in a streaming pipeline?",
    a: "Direct DB writes seem simpler, so why add Kafka in the middle?\n\nPROBLEM with direct writes:\n- If the DB is slow or down, your producer is blocked — you lose events\n- You can't have multiple consumers reading the same stream at different speeds\n- No replay: once written, the DB event is 'consumed' — can't re-process it\n- High write volume can overwhelm the DB\n\nKafka solves all of this:\n\n1. DECOUPLING: producers and consumers are independent. Producer writes to Kafka at full speed. Consumer reads at its own pace.\n\n2. BUFFERING: Kafka absorbs traffic spikes. If your consumer falls behind, events wait in Kafka (configurable retention: hours, days, weeks)\n\n3. MULTIPLE CONSUMERS: many services can read the same topic independently\n   - Consumer 1: writes to data warehouse\n   - Consumer 2: triggers real-time alerts\n   - Consumer 3: feeds ML model\n\n4. REPLAY: if your consumer has a bug, fix it and re-read from any point in history\n\n5. DURABILITY: messages are persisted to disk across multiple brokers\n\nKafka is not always needed — for low-volume, simple use cases, direct DB writes are fine."
  },
  {
    id: 13, tag: "WHY NOT",
    q: "Why use a columnar database (like Redshift or BigQuery) instead of MySQL for analytics?",
    a: "MySQL (and Postgres) are ROW-BASED databases — optimized for OLTP (Online Transaction Processing).\nRedshift/BigQuery are COLUMNAR databases — optimized for OLAP (Online Analytical Processing).\n\nImagine a table with 100 columns and 1 billion rows.\n\nQuery: SELECT SUM(revenue) FROM orders WHERE year = 2023\n\nMySQL approach:\n- Reads all 100 columns for all 1 billion rows to find 'revenue'\n- Then filters by year\n- Result: reads ~100TB of data for a simple sum\n\nRedshift approach:\n- Only reads the 'revenue' column and 'year' column from disk\n- Skips all other 98 columns entirely\n- Those two columns compress extremely well (same data type)\n- Result: reads maybe 2GB of compressed data — 50,000x less I/O\n\nAdditional OLAP advantages:\n- Massively parallel processing across hundreds of nodes\n- No row locks, no index maintenance overhead\n- Designed for full table scans, aggregations, and joins on billions of rows\n\nConversely: never use BigQuery as your app's transactional database — it has no row-level updates and high latency per query."
  },

  // ─── SQL ───────────────────────────────────────────────────────────────────
  {
    id: 14, tag: "SQL",
    q: "What is a window function? Give a practical example.",
    a: "A window function performs a calculation across a set of rows RELATED to the current row — without collapsing them into a single result like GROUP BY does.\n\nSyntax:\n  function() OVER (PARTITION BY col ORDER BY col ROWS/RANGE ...)\n\nPractical example — rank customers by revenue within each country:\n  SELECT\n    customer_name,\n    country,\n    revenue,\n    RANK() OVER (PARTITION BY country ORDER BY revenue DESC) AS country_rank\n  FROM orders;\n\nResult: each row keeps its full detail PLUS the rank within its country group.\n\nCommon window functions:\n  ROW_NUMBER()  — unique sequential number (no ties)\n  RANK()        — ties get same rank, next rank skips (1,1,3)\n  DENSE_RANK()  — ties get same rank, no skip (1,1,2)\n  LAG(col, 1)   — value from previous row\n  LEAD(col, 1)  — value from next row\n  SUM() OVER () — running total\n  AVG() OVER () — moving average\n\nWhen to use vs GROUP BY:\n- GROUP BY: you want one row per group (aggregation loses detail)\n- Window function: you want the aggregate AND the detail row"
  },
  {
    id: 15, tag: "SQL",
    q: "What is a CTE (Common Table Expression) and when would you use it over a subquery?",
    a: "A CTE is a named temporary result set defined with the WITH keyword that you can reference in the main query.\n\nExample — find customers whose revenue is above the average:\n\n  -- Subquery version (hard to read):\n  SELECT customer_name\n  FROM orders\n  WHERE revenue > (SELECT AVG(revenue) FROM orders);\n\n  -- CTE version (clean and readable):\n  WITH avg_revenue AS (\n    SELECT AVG(revenue) AS avg_rev FROM orders\n  )\n  SELECT o.customer_name\n  FROM orders o, avg_revenue a\n  WHERE o.revenue > a.avg_rev;\n\nWhy CTEs over subqueries:\n1. READABILITY: complex logic broken into named steps — reads like a story\n2. REUSABILITY: reference the same CTE multiple times in one query (subquery must be repeated)\n3. RECURSIVE CTEs: CTEs can call themselves — impossible with subqueries (great for hierarchies like org charts)\n4. DEBUGGING: you can comment out later steps and inspect intermediate results\n\nPerformance note: in most databases, CTEs and subqueries perform the same — the optimizer treats them identically. Snowflake and BigQuery materialize some CTEs differently, so test if performance matters."
  },
  {
    id: 16, tag: "SQL",
    q: "Explain the difference between INNER JOIN, LEFT JOIN, and FULL OUTER JOIN with a real scenario.",
    a: "Scenario: orders table and customers table. Some customers have no orders. Some orders have no matching customer (data quality issue).\n\nINNER JOIN — only matching rows from BOTH tables:\n  SELECT o.order_id, c.customer_name\n  FROM orders o\n  INNER JOIN customers c ON o.customer_id = c.customer_id\n  -- Only returns orders that have a matching customer\n  -- Orphan orders dropped. Customers without orders dropped.\n\nLEFT JOIN — ALL rows from left table, matched rows from right:\n  SELECT o.order_id, c.customer_name\n  FROM orders o\n  LEFT JOIN customers c ON o.customer_id = c.customer_id\n  -- All orders returned. If no matching customer, customer_name = NULL\n  -- Use this to find orphan orders: WHERE c.customer_id IS NULL\n\nFULL OUTER JOIN — ALL rows from BOTH tables:\n  SELECT o.order_id, c.customer_name\n  FROM orders o\n  FULL OUTER JOIN customers c ON o.customer_id = c.customer_id\n  -- Returns everything: matched rows, unmatched orders, AND unmatched customers\n  -- Great for data reconciliation / finding mismatches\n\nInterview tip: Always ask 'what should happen to unmatched rows?' — that determines which join to use."
  },
  {
    id: 17, tag: "SQL",
    q: "What is the difference between WHERE and HAVING?",
    a: "Short answer: WHERE filters ROWS before aggregation. HAVING filters GROUPS after aggregation.\n\nOrder of SQL execution:\n  FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY\n\nExample — find countries with total revenue over 10,000:\n\n  SELECT country, SUM(revenue) AS total_rev\n  FROM orders\n  WHERE status = 'completed'       -- filters individual rows FIRST\n  GROUP BY country\n  HAVING SUM(revenue) > 10000;     -- filters groups AFTER aggregation\n\nWHY you can't use WHERE on an aggregate:\n  -- This will ERROR:\n  WHERE SUM(revenue) > 10000\n  -- SUM() doesn't exist yet at the WHERE stage\n\nCommon interview trick question:\n  'Can you use an alias from SELECT in HAVING?'\n  -- In most databases: NO (HAVING runs before SELECT)\n  -- Exception: BigQuery and some others allow it\n  -- Safe answer: repeat the expression in HAVING"
  },
  {
    id: 18, tag: "SQL",
    q: "What is query optimization? What do you look for in an EXPLAIN plan?",
    a: "Query optimization is finding the most efficient way to execute a SQL query — same result, less time and resources.\n\nThe EXPLAIN command shows you the execution plan — what the database ACTUALLY does to run your query.\n\n  EXPLAIN SELECT * FROM orders WHERE customer_id = 'C001';\n\nWhat to look for in an EXPLAIN plan:\n\n1. SEQUENTIAL SCAN (bad on large tables)\n   - 'Seq Scan on orders' = reading every single row\n   - Needs an index: CREATE INDEX ON orders(customer_id);\n\n2. INDEX SCAN (good)\n   - 'Index Scan using orders_customer_id_idx' = using index to jump directly to rows\n\n3. NESTED LOOP vs HASH JOIN vs MERGE JOIN\n   - Nested Loop: good for small tables or indexed joins\n   - Hash Join: good for large unsorted tables\n   - Merge Join: good when both sides are already sorted\n\n4. COST estimate (rows, width, total cost)\n   - Higher cost = slower. Compare before/after adding an index.\n\n5. SORT operations — expensive. Can often be avoided with an index\n\nCommon optimizations:\n  - Add indexes on columns used in WHERE, JOIN, ORDER BY\n  - Avoid SELECT * — only select columns you need\n  - Filter early with WHERE before JOINs\n  - Use partitioning so the engine skips entire partitions"
  },
  {
    id: 19, tag: "SQL",
    q: "Write a query to find the top 3 products by revenue in each country.",
    a: "-- Using ROW_NUMBER() window function:\n\nWITH ranked AS (\n  SELECT\n    country,\n    product,\n    SUM(revenue) AS total_revenue,\n    ROW_NUMBER() OVER (\n      PARTITION BY country\n      ORDER BY SUM(revenue) DESC\n    ) AS rn\n  FROM orders\n  WHERE status = 'completed'\n  GROUP BY country, product\n)\nSELECT country, product, total_revenue\nFROM ranked\nWHERE rn <= 3\nORDER BY country, total_revenue DESC;\n\nWhy ROW_NUMBER() not RANK():\n- ROW_NUMBER() always gives exactly 3 rows per country (unique numbers)\n- RANK() could give 4+ rows if there's a tie for 3rd place\n- DENSE_RANK() would also give extra rows on ties\n\nChoose based on business rule:\n  'Exactly top 3 slots' → ROW_NUMBER()\n  'Top 3 ranks, ties included' → RANK() or DENSE_RANK()"
  },
  {
    id: 20, tag: "SQL",
    q: "What is the difference between UNION and UNION ALL?",
    a: "UNION: combines results from two queries and REMOVES duplicates\nUNION ALL: combines results and KEEPS all rows including duplicates\n\nExample:\n  Table A: [1, 2, 3]\n  Table B: [2, 3, 4]\n\n  A UNION B     → [1, 2, 3, 4]       (duplicates removed)\n  A UNION ALL B → [1, 2, 3, 2, 3, 4] (all rows kept)\n\nPerformance difference — this is critical in interviews:\n  UNION performs a DISTINCT operation after combining = sorts all rows to find duplicates = expensive\n  UNION ALL just appends = no extra sort step = MUCH faster\n\nWhen to use each:\n  UNION ALL (almost always preferred):\n  - You KNOW there are no duplicates (different date ranges, different sources)\n  - You want duplicates (stacking logs from multiple servers)\n  - Performance matters\n\n  UNION (only when needed):\n  - You genuinely need to deduplicate across the two result sets\n\nInterview tip: if you write UNION in a query, the interviewer will often ask 'why not UNION ALL?' — always justify your choice."
  },

  // ─── DATA MODELING ─────────────────────────────────────────────────────────
  {
    id: 21, tag: "MODELING",
    q: "What is the difference between a star schema and a snowflake schema?",
    a: "Both are dimensional modeling patterns for data warehouses.\n\nSTAR SCHEMA:\n- One central FACT table surrounded by DENORMALIZED dimension tables\n- Dimension tables are flat (all attributes in one table)\n- Shape looks like a star\n\n  orders_fact (order_id, date_id, customer_id, product_id, revenue, quantity)\n  customer_dim (customer_id, name, city, country, region)  -- all in one table\n  product_dim (product_id, name, category, subcategory)    -- all in one table\n\nSNOWFLAKE SCHEMA:\n- Fact table surrounded by NORMALIZED dimension tables\n- Dimensions are split into sub-dimensions (normalized)\n\n  customer_dim (customer_id, name, city_id)  -- city split out\n  city_dim (city_id, city, country_id)       -- country split out\n  country_dim (country_id, country, region)  -- region split out\n\nStar Schema pros: simpler queries, fewer JOINs, faster analytics\nSnowflake Schema pros: less storage redundancy, easier to update dimension values\n\nFor analytics/BI: Star Schema is almost always preferred.\nFor strict storage efficiency or OLTP: Snowflake schema.\n\nMost real data warehouses use Star Schema."
  },
  {
    id: 22, tag: "MODELING",
    q: "What is the difference between a fact table and a dimension table?",
    a: "FACT TABLE:\n- Stores measurable, quantitative business events\n- Contains numeric metrics (measures) and foreign keys to dimension tables\n- Grows continuously — millions/billions of rows\n- Examples: orders, transactions, clicks, page_views, sensor_readings\n\n  orders_fact:\n  | order_id | date_id | customer_id | product_id | revenue | quantity |\n  | 1001     | 20230115| C001        | P042       | 899.99  | 2        |\n\nDIMENSION TABLE:\n- Stores descriptive context about the facts\n- Contains text attributes that give meaning to the numbers\n- Relatively small and stable — thousands to millions of rows\n- Examples: customers, products, dates, locations, employees\n\n  customer_dim:\n  | customer_id | name  | city     | country | segment |\n  | C001        | Alice | New York | USA     | Premium |\n\nMemory trick:\n- Fact table answers: HOW MUCH? HOW MANY? (numbers)\n- Dimension tables answer: WHO? WHAT? WHERE? WHEN? (context)\n\nDate dimension is a special, very important dimension — pre-populated with every date for years and includes: day_of_week, is_weekend, is_holiday, quarter, fiscal_year, etc."
  },
  {
    id: 23, tag: "MODELING",
    q: "What is data normalization? What are 1NF, 2NF, and 3NF?",
    a: "Normalization is the process of organizing a database to reduce redundancy and improve data integrity.\n\n1NF (First Normal Form):\n- Each column contains atomic (indivisible) values\n- No repeating groups or arrays in a column\n- Violation: storing 'Alice,Bob,Charlie' in one cell\n  Fix: one row per value\n\n2NF (Second Normal Form):\n- Must be in 1NF\n- Every non-key column depends on the WHOLE primary key (no partial dependency)\n- Only relevant for tables with COMPOSITE primary keys\n- Violation: order table with (order_id, product_id) as PK, but 'product_name' only depends on product_id\n  Fix: move product_name to a separate products table\n\n3NF (Third Normal Form):\n- Must be in 2NF\n- No transitive dependencies: non-key columns should not depend on other non-key columns\n- Violation: orders table has customer_id, customer_city, customer_country — country depends on city, not on order\n  Fix: move city/country to customers table\n\nFor data warehouses: we deliberately DENORMALIZE (violate 3NF) to avoid JOINs and speed up queries.\nFor transactional databases (OLTP): we want high normalization to avoid update anomalies."
  },
  {
    id: 24, tag: "MODELING",
    q: "What is partitioning in a data warehouse and why is it important?",
    a: "Partitioning divides a large table into smaller, physical segments (partitions) based on the values of one or more columns — usually a date column.\n\nExample: orders table partitioned by month\n  orders/year=2023/month=01/data.parquet\n  orders/year=2023/month=02/data.parquet\n  orders/year=2023/month=03/data.parquet\n\nWhy it matters — PARTITION PRUNING:\n  Query: SELECT * FROM orders WHERE order_date >= '2023-06-01'\n  Without partitioning: scans ALL data across ALL years\n  With partitioning: scans ONLY the June 2023+ partitions — skips everything before\n\nBig performance and cost win — especially in cloud warehouses that charge per byte scanned (BigQuery).\n\nCommon partition columns:\n- date (most common: daily, monthly, yearly)\n- region or country (for geo-sharded data)\n- status or category (for skewed data, be careful)\n\nPartitioning vs Indexing:\n- Index: fast lookup for specific rows (OLTP, transactional)\n- Partitioning: fast pruning of entire data chunks (OLAP, analytical)\n\nPitfall: Over-partitioning (e.g., partitioning by hour on a small table) creates too many tiny files — 'small file problem'. Always partition by the column most commonly used in WHERE filters."
  },
  {
    id: 25, tag: "WHY NOT",
    q: "Why use a surrogate key instead of a natural key in a dimension table?",
    a: "NATURAL KEY: the real-world identifier (email, SSN, order_number, product_code)\nSURROGATE KEY: a system-generated artificial integer (usually an auto-increment ID)\n\nProblems with natural keys as primary keys:\n\n1. THEY CHANGE: emails change, product codes get reused, SSNs get corrected\n   - If you use email as PK and someone changes their email, you must update every fact table row that references it — millions of rows\n   - Surrogate key never changes\n\n2. THEY EXPOSE BUSINESS LOGIC: natural keys sometimes encode logic (product code 'ELEC-2024-001' tells you category and year)\n   - When logic changes, keys break\n\n3. SCD TYPE 2 REQUIRES IT: to track history, the same customer (same natural key) gets multiple rows — you need a surrogate key to uniquely identify each version\n   | surrogate_id | customer_id | city | is_current |\n   | 1001         | C001        | NYC  | False      |\n   | 2847         | C001        | LA   | True       |\n\n4. JOIN PERFORMANCE: integers join faster than long strings\n\n5. SOURCE SYSTEM INDEPENDENCE: if you integrate two source systems with conflicting natural keys, surrogate keys resolve conflicts cleanly\n\nRule: always use surrogate keys in dimensional models. Keep natural keys as business/lookup columns, not as primary keys."
  },

  // ─── FILE FORMATS ───────────────────────────────────────────────────────────
  {
    id: 26, tag: "FILE FORMATS",
    q: "What is the difference between Parquet, Avro, ORC, and JSON? When do you use each?",
    a: "JSON:\n- Human-readable, flexible schema, row-based\n- Great for: APIs, configs, raw ingestion of semi-structured data\n- Bad for: analytics at scale (slow reads, no compression efficiency)\n\nCSV:\n- Row-based, no types, no schema\n- Great for: small file exports, sharing with humans/Excel\n- Bad for: anything analytical at scale\n\nAVRO:\n- Row-based binary format with schema stored in the file (JSON schema header)\n- Great for: Kafka messages, write-heavy workloads, schema evolution\n- Schema is embedded: you can always reconstruct the data even without external schema registry\n- Supports schema evolution (add/remove fields without breaking consumers)\n\nPARQUET:\n- Columnar binary format\n- Great for: analytical reads, data lakes, Spark/BigQuery workloads\n- Best for: read-heavy, aggregation-heavy use cases\n- Supports predicate pushdown and column pruning\n\nORC (Optimized Row Columnar):\n- Also columnar, similar to Parquet\n- Native format for Hive, better for certain Hive workloads\n- Parquet is generally preferred in modern stacks\n\nMemory trick:\n- Writing events to Kafka? → Avro\n- Storing analytics data in S3? → Parquet\n- Exporting to a business user? → CSV\n- Ingesting raw API data? → JSON"
  },
  {
    id: 27, tag: "FILE FORMATS",
    q: "What is schema evolution and why does it matter in data engineering?",
    a: "Schema evolution is the ability of a data format or system to handle changes to the structure of data over time — without breaking existing pipelines or readers.\n\nReal scenario:\nYour orders table starts with: (order_id, customer_id, revenue)\nNext month, the app adds a new field: discount_code\nThe month after, they remove phone_number\n\nWithout schema evolution:\n- Old files have different structure from new files\n- Your pipeline breaks every time the schema changes\n- You need to reprocess everything\n\nWith schema evolution:\n- Old files missing the new column → readers return NULL for that column (forward compatibility)\n- New files missing an old column → readers use a default value (backward compatibility)\n\nFormats that handle schema evolution well:\n- Avro: schema stored in the file; schema registry tracks versions\n- Parquet: column-based, so adding/removing columns is clean\n- Delta Lake / Iceberg: full schema evolution with ALTER TABLE support\n\nDelta Lake example:\n  ALTER TABLE orders ADD COLUMN discount_code STRING;\n  -- Existing rows automatically get NULL for discount_code\n\nBad at schema evolution:\n- CSV: no types, no schema — any change can silently corrupt your pipeline\n- JSON: flexible but readers must handle all possible shapes\n\nKey interview point: in production, schema changes are one of the most common causes of pipeline failures. Good schemas are designed with evolution in mind."
  },

  // ─── DATABASES ─────────────────────────────────────────────────────────────
  {
    id: 28, tag: "DATABASES",
    q: "What is the difference between OLTP and OLAP databases?",
    a: "OLTP (Online Transaction Processing):\n- Optimized for: fast reads/writes of individual rows (your app's database)\n- Queries: INSERT, UPDATE, DELETE, SELECT by primary key\n- Row-based storage\n- Highly normalized (3NF)\n- Concurrent users: thousands of app users\n- Scale: gigabytes to terabytes\n- Examples: PostgreSQL, MySQL, Oracle, SQL Server\n- Example query: 'Get order #1042 for Alice' (point lookup)\n\nOLAP (Online Analytical Processing):\n- Optimized for: fast aggregations across millions/billions of rows\n- Queries: SELECT, GROUP BY, JOINs across large tables\n- Columnar storage\n- Denormalized (star/snowflake schema)\n- Concurrent users: dozens of analysts\n- Scale: terabytes to petabytes\n- Examples: BigQuery, Redshift, Snowflake, ClickHouse\n- Example query: 'Sum revenue by country for the last 12 months'\n\nCritical distinction:\n- OLTP: optimized for writes, single-row lookups\n- OLAP: optimized for reads, full table scans, aggregations\n\nNEVER run heavy analytics on your OLTP database — it will lock rows, degrade app performance, and make your DBA cry. Always replicate data to a separate analytics system."
  },
  {
    id: 29, tag: "DATABASES",
    q: "What is an index and when should you use one?",
    a: "An index is a separate data structure (usually a B-tree) that the database maintains to enable fast lookups without scanning every row.\n\nWithout index:\n  SELECT * FROM orders WHERE customer_id = 'C001';\n  → Database reads EVERY row top to bottom until it finds matches = O(n)\n  On 100M rows: might take minutes\n\nWith index on customer_id:\n  → Database jumps directly to matching rows using the B-tree = O(log n)\n  On 100M rows: might take milliseconds\n\nWhen TO use indexes:\n- Columns frequently used in WHERE clauses\n- Columns used in JOIN conditions\n- Columns used in ORDER BY (if you often sort by them)\n- Primary keys (automatically indexed)\n- Foreign keys (important for JOIN performance)\n\nWhen NOT to use indexes:\n- Columns with very low cardinality (e.g., status with only 3 values — a full scan might be faster)\n- Tables that are written to very frequently (indexes slow down INSERT/UPDATE because they must be maintained)\n- Small tables (full scan is fast anyway)\n- Analytics / OLAP workloads (columnar storage + partitioning is better than indexes)\n\nIndex types:\n- B-tree: default, great for equality and range queries\n- Hash: only equality (=), faster than B-tree for exact matches\n- Partial index: index only rows matching a condition (e.g., WHERE status = 'active')\n- Composite index: index on multiple columns — column ORDER matters"
  },
  {
    id: 30, tag: "DATABASES",
    q: "What is ACID compliance and why does it matter?",
    a: "ACID is a set of properties that guarantee database transactions are processed reliably.\n\nA — ATOMICITY:\n'All or nothing.' A transaction either fully completes or fully rolls back.\nExample: transferring $100 from A to B:\n  - Debit A: -$100\n  - Credit B: +$100\nIf the system crashes after step 1, atomicity ensures the debit is rolled back. Money doesn't disappear.\n\nC — CONSISTENCY:\nThe database is always in a valid state before and after a transaction.\nConstraints (foreign keys, NOT NULL, UNIQUE) are never violated.\n\nI — ISOLATION:\nConcurrent transactions don't interfere with each other.\nIf two people buy the last item simultaneously, isolation prevents overselling.\nLevels: READ UNCOMMITTED → READ COMMITTED → REPEATABLE READ → SERIALIZABLE (strictest)\n\nD — DURABILITY:\nOnce a transaction is committed, it stays committed — even if the server crashes immediately after.\nData is written to disk (WAL log) before the commit is acknowledged.\n\nWhy it matters for data engineering:\n- OLTP databases are fully ACID — critical for financial, e-commerce apps\n- Traditional data lakes (raw S3 files) are NOT ACID — concurrent writes can corrupt data\n- Delta Lake, Iceberg, Hudi bring ACID to data lakes by adding transaction logs on top of Parquet files\n- This is why the lakehouse pattern is so powerful: lake storage + ACID guarantees"
  },

  // ─── SPARK ─────────────────────────────────────────────────────────────────
  {
    id: 31, tag: "SPARK",
    q: "What is Apache Spark and what problems does it solve?",
    a: "Apache Spark is a distributed computing engine for processing large datasets in parallel across a cluster of machines.\n\nThe problem it solves:\n- Some datasets are too big to fit on one machine (terabytes, petabytes)\n- Processing 1TB of data on a single laptop: hours or impossible\n- Spark splits the data and work across 100s of machines: minutes\n\nKey Spark concepts:\n\n1. RDD (Resilient Distributed Dataset): low-level distributed collection\n2. DataFrame: higher-level table-like API (preferred, like pandas but distributed)\n3. Lazy evaluation: operations are NOT executed when you write them — Spark builds an execution plan and runs it all at once when you call .show() or .write()\n   - This lets Spark optimize the whole plan before running\n\n4. Transformations (lazy): map, filter, groupBy, join — build the plan\n5. Actions (trigger execution): count(), show(), write() — execute the plan\n\nSpark runs on:\n- Local machine (for development)\n- YARN / Kubernetes cluster\n- Databricks (managed Spark)\n- EMR on AWS\n\nSpark vs Pandas:\n- Pandas: single machine, in-memory, up to ~10GB comfortable\n- Spark: distributed cluster, handles terabytes/petabytes\n- PySpark API is very similar to pandas — easy to learn"
  },
  {
    id: 32, tag: "SPARK",
    q: "What is a Spark partition and what is data skew?",
    a: "PARTITION:\nSpark divides data into partitions — smaller chunks that can be processed in parallel by different workers.\n\n  1 partition = 1 task = processed by 1 CPU core\n\nIf you have 100 partitions and 20 cores, Spark runs 20 tasks in parallel, then 20 more, etc.\n\nToo few partitions: workers sit idle, no parallelism\nToo many partitions: overhead of managing thousands of tiny tasks\nGolden rule: aim for partition size of 100-200 MB\n\nDATA SKEW:\nSkew happens when data is unevenly distributed across partitions.\n\nExample: you join orders with customers, partitioned by country. 80% of orders are from USA. One worker gets the massive USA partition and takes 10x longer while all others finish and sit idle.\n\nResult: your 100-worker cluster effectively runs at the speed of 1 worker.\n\nFixes for skew:\n1. SALTING: add a random suffix to the skewed key to spread data\n   customer_id + '_' + random(0,10) → distributes USA into 10 sub-partitions\n\n2. Broadcast join: if one table is small, broadcast it to all workers — no shuffle needed\n   from pyspark.sql.functions import broadcast\n   df.join(broadcast(small_df), 'id')\n\n3. Repartition on a better key: repartition(200) or repartition('better_column')\n\nData skew is one of the most common Spark performance problems in production."
  },
  {
    id: 33, tag: "SPARK",
    q: "What is the difference between repartition() and coalesce() in Spark?",
    a: "Both change the number of partitions, but they work very differently.\n\nREPARTITION(n):\n- Can INCREASE or DECREASE partition count\n- Performs a FULL SHUFFLE: redistributes ALL data across all nodes\n- Expensive — moves data over the network\n- Gives evenly balanced partitions\n\n  df.repartition(200)             # 200 evenly-sized partitions\n  df.repartition(200, 'country')  # partition BY country column\n\nCOALESCE(n):\n- Can only DECREASE partition count\n- NO full shuffle: merges nearby partitions on the same node\n- Much cheaper than repartition\n- BUT: partitions may be uneven (larger partitions)\n\n  df.coalesce(10)  # reduce to 10 partitions, minimal data movement\n\nWhen to use each:\n- Writing final output to fewer files? → coalesce (cheap)\n- Data is skewed and you want to rebalance? → repartition\n- Need to partition by a column for downstream joins? → repartition\n- Just reducing files before writing to S3? → coalesce(1) or coalesce(10)\n\nCommon pattern:\n  df.coalesce(1).write.parquet('s3://bucket/output/')  # write one clean file"
  },
  {
    id: 34, tag: "WHY NOT",
    q: "Why does Spark use lazy evaluation? Why not execute operations immediately like pandas?",
    a: "Lazy evaluation means Spark does NOT run transformations immediately. It builds up a logical plan and only executes when an action is called.\n\nWhy this is brilliant:\n\n1. WHOLE-PLAN OPTIMIZATION (Catalyst Optimizer):\n   Spark sees the ENTIRE chain of operations at once and can optimize:\n   - Push filters early: if you filter AFTER a join, Spark may move the filter BEFORE the join to reduce data size before the expensive operation\n   - Skip columns: if you only use 3 columns at the end, Spark won't read the other 97 from disk\n   - Combine operations: multiple maps can be fused into one pass over the data\n\n2. AVOID UNNECESSARY WORK:\n   df.filter(...).limit(10).show()\n   Spark doesn't need to filter ALL 1 billion rows — it stops as soon as it finds 10 matches.\n   Immediate execution would filter everything first.\n\n3. FAULT TOLERANCE:\n   Because the plan is stored (as a DAG), if a partition fails mid-execution, Spark recomputes only that partition by re-executing the relevant part of the plan.\n\nPandas executes immediately because it's single-machine — the dataset fits in memory, so optimization matters less. Spark operates across hundreds of machines where unnecessary data movement costs minutes and dollars.\n\nIn practice: always cache() a DataFrame if you use it multiple times. Without caching, lazy evaluation means it gets recomputed from scratch each time an action is called."
  },

  // ─── ORCHESTRATION ─────────────────────────────────────────────────────────
  {
    id: 35, tag: "ORCHESTRATION",
    q: "What is Apache Airflow and what problem does it solve?",
    a: "Airflow is a workflow orchestration platform — it schedules, monitors, and manages the execution of data pipelines.\n\nThe problem without Airflow:\n- You have a pipeline: ingest → transform → load → notify\n- Each step depends on the previous one\n- You use cron jobs, but: how do you know if step 2 failed? How do you retry? How do you see history? How do you handle dependencies between pipelines?\n\nAirflow solves this with DAGs (Directed Acyclic Graphs):\n- A DAG defines your pipeline as a graph of tasks with dependencies\n- Each node is a task (PythonOperator, BashOperator, SparkSubmitOperator, etc.)\n- Edges define dependencies: 'task B runs only after task A succeeds'\n\nAirflow gives you:\n1. SCHEDULING: run every day at 2am, every hour, every 15 minutes\n2. DEPENDENCY MANAGEMENT: task B waits for task A\n3. MONITORING: visual UI showing every DAG run, every task status\n4. RETRY LOGIC: automatically retry failed tasks N times with backoff\n5. ALERTING: email/Slack on failure\n6. BACKFILLING: re-run historical dates after fixing a bug\n\nKey Airflow concepts:\n- DAG: the pipeline definition\n- Task: a single unit of work\n- Operator: template for a task type\n- Sensor: waits for a condition (file exists, DB has new rows, etc.)\n- XCom: pass small data between tasks\n- Scheduler: triggers DAG runs based on schedule\n- Executor: runs the actual tasks (LocalExecutor, CeleryExecutor, KubernetesExecutor)"
  },
  {
    id: 36, tag: "ORCHESTRATION",
    q: "What does backfilling mean in Airflow and why is it important?",
    a: "Backfilling is re-running a pipeline for past dates — either because the pipeline wasn't running yet, or because you fixed a bug and need to reprocess old data.\n\nExample scenario:\nYou built a revenue pipeline on January 1st. Today is March 1st. Your manager asks for revenue data for October-December (before the pipeline existed). You need to backfill those 3 months.\n\n  airflow dags backfill -s 2023-10-01 -e 2023-12-31 my_revenue_dag\n\nAirflow runs the DAG for every scheduled interval between those dates.\n\nWhy backfilling requires idempotency:\nIf you backfill a date that was already processed, it runs again. If your pipeline isn't idempotent (doesn't delete before inserting), you'll get duplicate data.\n\nAirflow execution_date:\nAirflow passes execution_date to each DAG run — this is the date the run is FOR, not when it actually runs. Your pipeline should use this date to determine which data to process:\n  # In Python operator:\n  def process(**context):\n      run_date = context['execution_date'].strftime('%Y-%m-%d')\n      query = f\"SELECT * FROM orders WHERE date = '{run_date}'\"\n\nThis design allows backfilling to work correctly — each historical run uses its own date.\n\nCatch-up: Airflow has a catchup=True setting that automatically runs missed intervals when a DAG is first enabled or unpaused. Often set to False to avoid accidental backfills."
  },
  {
    id: 37, tag: "ORCHESTRATION",
    q: "What is the difference between a task and a DAG in Airflow?",
    a: "DAG (Directed Acyclic Graph):\n- The PIPELINE as a whole — the container\n- Defines the schedule (daily, hourly, cron expression)\n- Defines the set of tasks and their dependencies\n- Has properties: dag_id, schedule_interval, start_date, catchup, tags\n\nTASK:\n- A SINGLE STEP within a DAG\n- Defined using an Operator\n- Has its own state: queued, running, success, failed, skipped, retrying\n\nExample DAG with 4 tasks:\n\n  with DAG('orders_pipeline', schedule_interval='@daily') as dag:\n\n      extract = PythonOperator(\n          task_id='extract_orders',\n          python_callable=extract_fn\n      )\n\n      transform = SparkSubmitOperator(\n          task_id='transform_orders',\n          application='transform.py'\n      )\n\n      load = PythonOperator(\n          task_id='load_to_warehouse',\n          python_callable=load_fn\n      )\n\n      notify = SlackOperator(\n          task_id='send_notification'\n      )\n\n      extract >> transform >> load >> notify\n      # >> means 'then': extract runs first, then transform, etc.\n\nACYCLIC means no circular dependencies:\n  A → B → C → A  # NOT allowed — would loop forever\n  A → B → C      # Allowed"
  },

  // ─── STREAMING ─────────────────────────────────────────────────────────────
  {
    id: 38, tag: "STREAMING",
    q: "What is Apache Kafka and what are its core components?",
    a: "Kafka is a distributed streaming platform — a high-throughput, fault-tolerant message queue designed for real-time data.\n\nCore components:\n\nTOPIC:\n- A named stream of records (like a table in a database, but for events)\n- 'orders-topic', 'page-views-topic', 'payment-events-topic'\n\nPRODUCER:\n- An application that writes (publishes) records to a topic\n- Example: your web app publishes an event every time an order is placed\n\nCONSUMER:\n- An application that reads (subscribes to) records from a topic\n- Example: your data pipeline reads order events and writes to the warehouse\n\nCONSUMER GROUP:\n- Multiple consumers sharing the work of reading a topic\n- Each partition is assigned to only ONE consumer in the group\n- Scale reading by adding more consumers (up to partition count)\n\nPARTITION:\n- Topics are split into ordered, immutable partitions\n- Parallelism unit: more partitions = more parallel consumers\n- Records in a partition maintain ORDER — across partitions, order is not guaranteed\n\nBROKER:\n- A Kafka server. A cluster = multiple brokers\n- Each broker holds some partitions\n\nOFFSET:\n- A unique integer ID for each record within a partition\n- Consumers track which offset they've processed\n- Allows replay: reset offset to 0 to re-read everything from the beginning\n\nRETENTION:\n- Kafka retains messages for a configurable period (e.g., 7 days)\n- Unlike a queue, reading a message does NOT delete it"
  },
  {
    id: 39, tag: "STREAMING",
    q: "What are the three message delivery guarantees in Kafka?",
    a: "Delivery guarantees define what happens when things go wrong (network failure, broker crash, consumer crash).\n\nAT MOST ONCE:\n- Messages are delivered 0 or 1 times — never duplicated but can be lost\n- How: consumer commits offset BEFORE processing\n  If it crashes after commit but before processing → message lost forever\n- Use when: losing a few messages is OK (metrics, analytics counters)\n\nAT LEAST ONCE (most common):\n- Messages are delivered 1 or MORE times — no loss but possible duplicates\n- How: consumer commits offset AFTER successfully processing\n  If it crashes after processing but before committing → message is re-delivered\n- Your consumer must handle duplicates (idempotent processing)\n- Use when: you cannot afford to lose messages (orders, payments)\n\nEXACTLY ONCE:\n- Messages are delivered exactly once — no loss, no duplicates\n- The hardest to achieve\n- Kafka supports it with: idempotent producers + transactional APIs\n- Kafka Streams and Flink have built-in exactly-once semantics\n- Use when: financial transactions, inventory updates\n\nIn practice:\n- Most real systems use AT LEAST ONCE and make the consumer idempotent (use UPSERT instead of INSERT, or check for duplicate IDs before processing)\n- Exactly-once adds significant complexity and latency — only use when truly necessary"
  },
  {
    id: 40, tag: "STREAMING",
    q: "What is a watermark in stream processing and why is it needed?",
    a: "Problem: in the real world, events arrive out of order and late.\n\nExample: your phone goes offline. You generate 10 click events at 2:00pm. When you reconnect at 2:15pm, those events arrive at the processing system 15 minutes late.\n\nYour pipeline is computing 'clicks per minute'. When the 2:00pm window closes, it doesn't know those 10 events are still coming.\n\nWATERMARK:\nA watermark tells the streaming system: 'you can safely close a time window up to time T, because we won't see any events older than T anymore.'\n\nIt's defined as: watermark_time = max_event_time_seen - allowed_lateness\n\nExample in Spark Structured Streaming:\n  df.withWatermark('event_time', '15 minutes')  # allow up to 15 min of lateness\n    .groupBy(window('event_time', '1 minute'))\n    .count()\n\nThis tells Spark: 'don't close a window until 15 minutes after the window ends — to catch late events.'\n\nTrade-off:\n- Larger watermark: more lateness tolerated, more correct results, but results come later\n- Smaller watermark: faster results, but late events are dropped\n\nWithout watermarks:\n- You'd either wait forever (stream never emits results)\n- Or close windows too early and miss late events\n\nThis is one of the fundamental challenges of stream processing that doesn't exist in batch."
  },

  // ─── DATA QUALITY ──────────────────────────────────────────────────────────
  {
    id: 41, tag: "DATA QUALITY",
    q: "What is data quality and what are the key dimensions to measure it?",
    a: "Data quality means data is fit for its intended purpose. Poor quality data leads to wrong decisions, broken pipelines, and lost trust in analytics.\n\nKey dimensions:\n\n1. COMPLETENESS: are all expected values present?\n   - Are there nulls in required columns?\n   - Did all expected rows arrive? (row count checks)\n   Test: COUNT(*) = expected_count, COUNT(col) / COUNT(*) > 0.99\n\n2. ACCURACY: does the data reflect reality?\n   - revenue > 0\n   - date is not in the future\n   - age is between 0 and 130\n\n3. CONSISTENCY: same entity represented the same way everywhere?\n   - 'United States' vs 'USA' vs 'US'\n   - Customer ID in orders matches customer table\n\n4. UNIQUENESS: no unexpected duplicates?\n   - order_id should be unique\n   - One row per customer per day\n\n5. TIMELINESS: is the data fresh enough?\n   - Pipeline ran on time\n   - Latest record is within expected freshness window\n   - max(updated_at) > NOW() - INTERVAL '1 day'\n\n6. VALIDITY: does data conform to expected format/type?\n   - Email contains '@'\n   - Status is one of ['completed', 'pending', 'cancelled']\n   - Date is a valid date\n\nTools for data quality:\n- Great Expectations: define expectations as code, run as tests\n- dbt tests: test SQL models in the transformation layer\n- Monte Carlo / Soda: monitoring and anomaly detection in production"
  },
  {
    id: 42, tag: "DATA QUALITY",
    q: "What are dbt tests and how do you use them?",
    a: "dbt (data build tool) lets you define and run automated tests on your SQL models in the data warehouse.\n\nBuilt-in generic tests (add to schema.yml):\n\n  models:\n    - name: orders\n      columns:\n        - name: order_id\n          tests:\n            - unique           # no duplicate order_ids\n            - not_null         # every row has an order_id\n        - name: status\n          tests:\n            - accepted_values:\n                values: ['completed', 'pending', 'cancelled']\n        - name: customer_id\n          tests:\n            - relationships:\n                to: ref('customers')   # FK check: every customer_id exists in customers\n                field: customer_id\n\nCustom singular test (a SQL query that returns failing rows):\n\n  -- tests/revenue_must_be_positive.sql\n  SELECT order_id, revenue\n  FROM {{ ref('orders') }}\n  WHERE revenue < 0\n  -- If this query returns any rows, the test FAILS\n\nRun tests:\n  dbt test                    # run all tests\n  dbt test --select orders    # test just the orders model\n\nWhere to run tests:\n- In CI/CD: run before deploying model changes\n- After every pipeline run: run tests as a task in Airflow\n- Alert on failure: Slack notification when tests fail\n\ndbt tests give you confidence that your transformations are correct."
  },
  {
    id: 43, tag: "DATA QUALITY",
    q: "How do you handle late-arriving data in a batch pipeline?",
    a: "Late data arrives AFTER the pipeline has already processed the time window it belongs to.\n\nExample: orders from Jan 15th that arrive in your source system on Jan 20th (delayed due to 3rd-party sync).\n\nStrategies:\n\n1. LOOK-BACK WINDOW (most common):\n   Instead of only processing today's data, reprocess the last N days every run.\n   WHERE order_date >= CURRENT_DATE - INTERVAL '7 days'\n   Combined with DELETE + INSERT or UPSERT for idempotency.\n   Cost: you process 7 days of data every day, not just 1.\n\n2. REPROCESS ON DETECTION:\n   Have a monitoring job that detects late arrivals (new rows inserted with old dates).\n   Trigger a backfill for just the affected date partitions.\n\n3. ACCEPT AND FLAG:\n   Process late data and tag it: is_late = TRUE\n   Recompute affected aggregates on a schedule.\n\n4. PARTITION OVERWRITE:\n   If using Spark/Delta Lake, overwrite affected date partitions when late data arrives.\n   df.write.mode('overwrite').partitionBy('date').parquet('s3://...')\n\nKey questions to answer with stakeholders:\n- How late can data arrive? (minutes, hours, days?)\n- How accurate must results be? (real-time vs eventually correct)\n- What is the cost tolerance of reprocessing?\n\nThere is no single perfect solution — it's always a trade-off between latency, cost, and accuracy."
  },

  // ─── CLOUD ─────────────────────────────────────────────────────────────────
  {
    id: 44, tag: "CLOUD",
    q: "What is the difference between AWS S3, Redshift, and Glue in a data stack?",
    a: "These three AWS services play very different roles in a data platform:\n\nS3 (Simple Storage Service):\n- Object storage — like a hard drive in the cloud\n- Store anything: raw JSON, Parquet files, logs, CSVs, images\n- Extremely cheap (pennies per GB)\n- Your data LAKE lives here\n- Not a database — you can't query it directly (well, via Athena you can)\n\nREDSHIFT:\n- Fully managed columnar data WAREHOUSE\n- You load clean/structured data from S3 into Redshift\n- Run SQL queries on billions of rows at high speed\n- Expensive — you pay for cluster compute always-on\n- Your analysts query this for BI dashboards\n\nGLUE:\n- Managed ETL service + metadata catalog\n- Glue ETL: run Python/Spark jobs without managing a cluster\n- Glue Catalog: a central metadata store — knows what files are in S3, their schema, partitions\n- Other tools (Athena, Redshift Spectrum) use Glue Catalog to discover and query S3 data\n\nTypical flow:\n  Raw data arrives → stored in S3 (data lake)\n  Glue ETL job cleans and transforms it → writes Parquet back to S3\n  Glue Catalog registers the schema\n  Redshift COPY command loads data from S3 to warehouse\n  Analysts query Redshift in Tableau/Looker"
  },
  {
    id: 45, tag: "CLOUD",
    q: "What is infrastructure as code (IaC) and why do data engineers care?",
    a: "IaC means defining your infrastructure (servers, databases, queues, pipelines) as code files instead of manually clicking through cloud consoles.\n\nExample: instead of clicking to create a Redshift cluster, you write:\n\n  # Terraform example\n  resource 'aws_redshift_cluster' 'warehouse' {\n    cluster_identifier = 'analytics-warehouse'\n    node_type          = 'dc2.large'\n    number_of_nodes    = 4\n    database_name      = 'analytics'\n  }\n\nWhy data engineers care:\n\n1. REPRODUCIBILITY: spin up identical environments (dev, staging, prod) from the same code\n\n2. VERSION CONTROL: infrastructure changes are tracked in Git — you know exactly who changed what and when\n\n3. DISASTER RECOVERY: if your AWS account is compromised, run terraform apply and rebuild everything in 30 minutes instead of days\n\n4. COLLABORATION: team reviews infra changes in pull requests before they're applied\n\n5. COST CONTROL: easily audit and remove unused resources\n\nPopular IaC tools for data engineers:\n- Terraform: cloud-agnostic, very popular for AWS/GCP/Azure resources\n- Pulumi: IaC using real programming languages (Python, TypeScript)\n- AWS CDK: IaC using Python/TypeScript, AWS-specific\n- dbt: treats SQL transformations as code (not infra but same principle)\n\nModern data teams version-control: SQL models, Airflow DAGs, Spark jobs, AND infrastructure."
  },
  {
    id: 46, tag: "WHY NOT",
    q: "Why use Delta Lake or Apache Iceberg instead of plain Parquet files in S3?",
    a: "Plain Parquet files in S3 seem simple — what's missing?\n\nProblems with plain Parquet/S3:\n\n1. NO ACID TRANSACTIONS:\n   Two Spark jobs writing simultaneously can corrupt files\n   A reader mid-write sees partial/dirty data\n\n2. NO DELETES OR UPDATES:\n   Parquet is immutable. To update a row, you must rewrite the entire file.\n   GDPR 'right to be forgotten'? Must rewrite every file that contains that user.\n\n3. NO TIME TRAVEL:\n   Made a mistake in a pipeline? Cannot roll back. The data is overwritten and gone.\n\n4. SCHEMA CHANGES BREAK THINGS:\n   Add a column to a new file, old files don't have it — readers may fail.\n\n5. SMALL FILE PROBLEM:\n   Streaming writes create many tiny Parquet files → slow reads for analytics.\n\nDelta Lake / Iceberg / Hudi solve ALL of these:\n\n1. ACID: transaction log tracks every change — readers always see a consistent snapshot\n\n2. DELETES AND UPDATES:\n   DELETE FROM orders WHERE customer_id = 'C001';  -- soft delete via log\n   UPDATE orders SET status = 'cancelled' WHERE order_id = 1042;\n\n3. TIME TRAVEL:\n   SELECT * FROM orders VERSION AS OF 5;    -- BigQuery / Databricks syntax\n   SELECT * FROM orders TIMESTAMP AS OF '2024-01-01';\n\n4. SCHEMA EVOLUTION: ALTER TABLE ADD COLUMN works cleanly\n\n5. AUTO OPTIMIZE: compacts small files automatically (Delta Lake)\n\nFor any serious production data lake, use an open table format. Plain Parquet is only fine for static, append-only, single-writer datasets."
  },

  // ─── MORE WHY NOT ───────────────────────────────────────────────────────────
  {
    id: 47, tag: "WHY NOT",
    q: "Why use dbt instead of writing raw SQL scripts in a folder?",
    a: "Raw SQL scripts in a folder get the job done — but at scale they become a nightmare.\n\nProblems with raw SQL scripts:\n\n1. NO DEPENDENCY MANAGEMENT:\n   Script B depends on Script A. You must run them in the right order manually.\n   With 50 scripts, this becomes impossible to manage.\n\n2. NO TESTING:\n   How do you know your revenue calc is correct? You don't — unless you manually check.\n\n3. NO DOCUMENTATION:\n   What does this table contain? What's the grain? Who knows — go read the SQL.\n\n4. NO VERSION CONTROL INTEGRATION:\n   SQL files in a folder aren't inherently tested in CI/CD.\n\n5. CODE DUPLICATION:\n   The same date logic or revenue formula copied in 15 different scripts.\n\ndbt solves all of this:\n\n1. DEPENDENCY GRAPH: {{ ref('orders') }} automatically builds the DAG — dbt runs models in the right order\n\n2. BUILT-IN TESTING: unique, not_null, accepted_values, relationships — run with dbt test\n\n3. AUTO-DOCUMENTATION: dbt generates a browsable website with model descriptions, column descriptions, lineage graph\n\n4. MACROS: reusable Jinja SQL functions — define once, use everywhere\n\n5. CI/CD: dbt runs in GitHub Actions — test before merge, deploy on merge\n\n6. ENVIRONMENTS: profiles.yml points to dev warehouse for devs, prod warehouse in CI\n\ndbt doesn't replace SQL — it makes SQL production-grade."
  },
  {
    id: 48, tag: "WHY NOT",
    q: "Why use Airflow for scheduling instead of just cron jobs?",
    a: "Cron works great for simple cases. Here's where it breaks down at scale:\n\nCRON PROBLEMS:\n\n1. NO DEPENDENCY MANAGEMENT:\n   Cron doesn't know that job B should only run after job A succeeds.\n   You hack it with sleep() and hope for the best.\n\n2. NO VISIBILITY:\n   Did the job run? Did it succeed? You check the logs file... if it exists... on the right server.\n\n3. NO RETRY LOGIC:\n   Job fails at 2am. Nobody knows until Monday morning.\n\n4. NO BACKFILL:\n   You need to re-run last week's jobs after fixing a bug. Good luck.\n\n5. SINGLE POINT OF FAILURE:\n   One server runs all cron jobs. Server goes down = all pipelines stop.\n\n6. NO ALERTING:\n   No built-in way to Slack/email your team on failure.\n\nAirflow gives you everything missing:\n  - Visual DAG UI: see every run, every task status, every log\n  - Dependencies: task B waits for task A, with operators like TriggerDagRunOperator\n  - Retry: retries=3, retry_delay=timedelta(minutes=5)\n  - Backfill: one CLI command re-runs historical dates\n  - Distributed: multiple workers — no single point of failure\n  - Alerting: email_on_failure=True, or custom Slack callbacks\n  - SLA monitoring: alert if a DAG doesn't finish within expected time\n\nFor 1-2 simple scripts: cron is fine.\nFor production data platforms with multiple interdependent pipelines: use an orchestrator."
  },

  // ─── MORE ADVANCED ──────────────────────────────────────────────────────────
  {
    id: 49, tag: "FUNDAMENTALS",
    q: "What is a medallion architecture (Bronze/Silver/Gold)?",
    a: "Medallion architecture is a data lake design pattern with three quality layers, popularized by Databricks.\n\nBRONZE (Raw):\n- Exact copy of raw source data — no transformations\n- Append-only, never modify\n- May contain duplicates, errors, bad types\n- Format: JSON, CSV, or raw Parquet as received\n- Purpose: source of truth, audit trail, replay capability\n  S3://lake/bronze/orders/2024/01/15/raw_orders.json\n\nSILVER (Cleaned):\n- Cleaned, deduplicated, typed, validated data\n- Nulls handled, dates parsed, schema standardized\n- Still close to source — minimal business logic\n- Format: Parquet or Delta Lake\n  S3://lake/silver/orders/  (partitioned by date)\n\nGOLD (Business-Ready):\n- Aggregated, joined, business-logic applied\n- Ready for analysts, BI tools, ML models\n- Optimized for specific use cases\n- Examples: daily_revenue_by_country, customer_360, product_performance\n  S3://lake/gold/daily_revenue/\n\nWhy this pattern:\n- Bronze: if Silver has a bug, you can always reprocess from Bronze\n- Silver: multiple Gold tables built from one clean Silver source\n- Gold: optimized for consumption — no analyst needs to deal with raw data\n\nEach layer adds value and quality. Each layer is a checkpoint for debugging.\nThis is now the standard architecture for modern data lakehouses."
  },
  {
    id: 50, tag: "FUNDAMENTALS",
    q: "What is a data contract and why is it becoming important?",
    a: "A data contract is a formal, agreed-upon specification between data producers (the team that generates data) and data consumers (analysts, pipelines, ML teams) that defines:\n\n- Schema: column names, types, required/optional\n- Semantics: what does 'revenue' actually mean? Is it gross or net? Before or after tax?\n- SLAs: data will arrive by 7am daily, with at most 0.1% null rate\n- Ownership: who is responsible for this data?\n- Versioning: how will breaking changes be communicated?\n\nWhy it matters:\nWithout contracts: the app team renames a column and your pipelines silently break. Analysts use 'revenue' but it means different things in different tables. Nobody knows who owns what.\n\nWith contracts:\n  name: orders\n  version: 2.1.0\n  owner: payments-team\n  columns:\n    - name: revenue\n      type: float\n      description: Net revenue after discounts, before tax, in USD\n      not_null: true\n  sla:\n    freshness: 2 hours\n    availability: 99.9%\n\nBreaking changes require a new major version and advance notice.\n\nTools: Soda, Great Expectations, dbt contracts, OpenMetadata, custom Pydantic schemas\n\nData contracts shift the responsibility for data quality LEFT — to the producers — rather than having every consumer defensively validate everything themselves."
  },
  {
    id: 51, tag: "SQL",
    q: "What is the difference between ROW_NUMBER(), RANK(), and DENSE_RANK()? When does it matter?",
    a: "All three assign numbers to rows within a partition ordered by some column. The difference appears ONLY when there are TIES.\n\nExample: scores [100, 100, 90, 80]\n\n  ROW_NUMBER(): 1, 2, 3, 4\n  - Always unique — ties broken arbitrarily\n  - Use when you need exactly N rows (top-3 per category with no extras)\n\n  RANK(): 1, 1, 3, 4\n  - Ties get the same rank\n  - Next rank SKIPS (no rank 2 here)\n  - '2nd place' doesn't exist if two people tied for 1st\n  - Use in actual competitive rankings (sports, leaderboards)\n\n  DENSE_RANK(): 1, 1, 2, 3\n  - Ties get the same rank\n  - Next rank does NOT skip\n  - Use when you want 'top 3 distinct rank levels'\n\nPractical example — find all products in the top 3 ranks by sales:\n  RANK() <= 3 → might return 4+ rows if there are ties\n  ROW_NUMBER() <= 3 → always exactly 3 rows\n  DENSE_RANK() <= 3 → returns all products at rank 1, 2, or 3\n\nInterview question they love:\n'Give me the top 3 customers per country, but if there's a tie for 3rd, include all tied customers.'\nAnswer: DENSE_RANK() or RANK() with WHERE rank <= 3, not ROW_NUMBER()"
  },
  {
    id: 52, tag: "MODELING",
    q: "What is a grain in dimensional modeling and why must you define it first?",
    a: "The GRAIN of a fact table defines exactly what ONE ROW represents.\n\nExamples of grains:\n- 'One row per order' → order_id is the primary key\n- 'One row per order line item' → (order_id, product_id) is the key\n- 'One row per customer per day' → (customer_id, date_id) is the key\n- 'One row per sensor reading' → (sensor_id, timestamp) is the key\n\nWhy define grain FIRST:\nGrain determines everything else:\n- Which facts (measures) make sense at this level?\n- Which dimensions can be joined?\n- What queries are answerable?\n\nExample of grain confusion:\nYou mix order-level rows (total revenue per order) with line-item rows (revenue per product per order) in the same table. Now SUM(revenue) double-counts. Analysts get wrong numbers. Nobody can trust the data.\n\nGrain rules:\n1. Define grain in plain English BEFORE writing any SQL\n2. Every row in the fact table must represent EXACTLY that grain\n3. Never mix grains in one fact table — create separate fact tables\n4. Measures must be additive at the defined grain\n\nFact table types by grain:\n- Transaction facts: one row per discrete event (order, click, payment)\n- Snapshot facts: one row per entity per time period (inventory level per day)\n- Accumulating facts: one row per process instance, updated as process progresses (order lifecycle: created → shipped → delivered)\n\nInterviewers ask: 'What is the grain of your fact table?' — always have a clear answer."
  },
  {
    id: 53, tag: "STREAMING",
    q: "What is the difference between Kafka and a traditional message queue (like RabbitMQ)?",
    a: "Both move messages between producers and consumers, but they have fundamental architectural differences.\n\nTRADITIONAL QUEUE (RabbitMQ, SQS):\n- Message is DELETED once a consumer reads it\n- Each message consumed by ONE consumer\n- Queue is FIFO — messages pushed to consumers\n- Designed for task distribution and work queues\n- Example use: email sending service — each email processed by one worker, then done\n\nKAFKA:\n- Messages are RETAINED for a configurable time (days, weeks, forever)\n- Same message can be read by MANY independent consumers\n- Consumers pull at their own pace — track their own offset\n- Designed for event streaming and data integration\n- Example use: order events read by warehouse pipeline AND fraud detection AND marketing — independently\n\nKey differences:\n\n1. REPLAY: Kafka allows re-reading from any point. RabbitMQ: once consumed, gone.\n\n2. MULTIPLE CONSUMERS: Kafka natively supports many consumer groups reading the same data. RabbitMQ: complex routing needed.\n\n3. THROUGHPUT: Kafka handles millions of messages/second. RabbitMQ optimized for lower-volume task queues.\n\n4. ORDERING: Kafka guarantees order within a partition. RabbitMQ has weaker ordering guarantees.\n\nWhen to use RabbitMQ: task queues, job distribution, RPC (request/response)\nWhen to use Kafka: event streaming, data pipelines, audit logs, real-time analytics"
  },
  {
    id: 54, tag: "DATA QUALITY",
    q: "How would you monitor a production data pipeline for failures and data quality issues?",
    a: "A production pipeline needs monitoring at multiple layers:\n\n1. PIPELINE HEALTH MONITORING (Did it run? Did it succeed?):\n   - Airflow: set email_on_failure=True, use SLA miss callbacks\n   - Alert if DAG hasn't completed by expected time (e.g., daily pipeline must finish by 8am)\n   - Track run duration: alert if a job takes 3x longer than usual (may indicate data volume spike)\n\n2. DATA FRESHNESS:\n   - Query: SELECT MAX(updated_at) FROM orders\n   - Alert if latest record is older than expected: max_updated_at < NOW() - INTERVAL '2 hours'\n   - Tools: Monte Carlo, Metaplane, or a simple cron + Slack alert\n\n3. ROW COUNT MONITORING:\n   - Store expected row counts and compare: today's rows vs 7-day average\n   - Alert if: row_count < 0.8 * avg_7day OR row_count > 2 * avg_7day\n   - Sudden drops can mean a source system issue; spikes can mean duplicates\n\n4. DATA QUALITY CHECKS:\n   - Run dbt tests or Great Expectations after every pipeline run\n   - Check: null rates, uniqueness, value distributions, referential integrity\n   - Block downstream pipelines if checks fail\n\n5. METRIC MONITORING:\n   - Track business metrics: daily_revenue, order_count, avg_order_value\n   - Alert on anomalies: revenue dropped 50% vs yesterday (could be data issue or real business event)\n\n6. LOGGING + ALERTING:\n   - All pipeline logs to a centralized system (CloudWatch, Datadog, ELK)\n   - PagerDuty/Opsgenie for critical failures that need immediate response\n   - Slack for non-critical warnings\n\nRule: never let analysts discover data quality issues before your monitoring does."
  },
  {
    id: 55, tag: "WHY NOT",
    q: "Why use cloud-managed services (like BigQuery, Snowflake) instead of running your own Spark cluster?",
    a: "Self-managed clusters SEEM cheaper and more flexible. Here's why teams still choose managed services:\n\nSELF-MANAGED SPARK (e.g., on EC2):\nPros: full control, can tune everything, no vendor lock-in\nCons:\n- You manage cluster provisioning, scaling, upgrades, security patches\n- Pay for idle compute (cluster always on even when no jobs run)\n- Need a DevOps/infra team just to maintain it\n- Performance tuning requires deep Spark expertise\n- High upfront investment in knowledge and setup\n\nBIGQUERY / SNOWFLAKE (serverless / managed):\nPros:\n- Zero infrastructure management — focus on data, not servers\n- Auto-scaling: scales to handle any query, scales to zero when idle\n- Separation of storage and compute: pay only for queries you run\n- BigQuery: pay per TB scanned ($5/TB) — perfect for variable workloads\n- Built-in HA, backups, security, encryption\n- SQL interface: accessible to analysts without engineering help\n- Continuous updates without your involvement\n\nCons:\n- Vendor lock-in (SQL dialects differ)\n- Cost can be unpredictable with large unoptimized queries\n- Less control over execution\n\nWhen self-managed makes sense:\n- Very high, predictable compute needs (dedicated cluster is cheaper)\n- Strict data residency requirements\n- Complex ML workloads needing custom runtime environments\n- Large companies with dedicated infra teams\n\nFor most teams: managed services let your data engineers do data engineering, not sysadmin work."
  },
  {
    id: 56, tag: "SPARK",
    q: "What is caching in Spark and when should you use it?",
    a: "Caching (also called persisting) stores a DataFrame in memory (or disk) so it doesn't have to be recomputed from scratch each time it's used.\n\nRemember lazy evaluation: every time you call an action on a DataFrame, Spark re-executes the ENTIRE lineage from scratch. If you use the same DataFrame twice, it's computed twice.\n\nWithout cache — computed twice:\n  cleaned_df = raw_df.filter(...).join(...).withColumn(...)\n  count = cleaned_df.count()       # executes full pipeline\n  result = cleaned_df.groupBy(...) # executes full pipeline AGAIN\n\nWith cache — computed once:\n  cleaned_df = raw_df.filter(...).join(...).withColumn(...)\n  cleaned_df.cache()               # marks for caching\n  count = cleaned_df.count()       # executes + stores in memory\n  result = cleaned_df.groupBy(...) # reads from memory — fast!\n\nCache levels:\n  df.cache()                                  # MEMORY_AND_DISK\n  df.persist(StorageLevel.MEMORY_ONLY)        # memory only, recompute if evicted\n  df.persist(StorageLevel.MEMORY_AND_DISK)    # spill to disk if memory full\n  df.persist(StorageLevel.DISK_ONLY)          # always on disk\n\nWhen TO cache:\n- DataFrame is used 2+ times in the same job\n- DataFrame is expensive to compute (heavy joins, many transformations)\n- Iterative algorithms (ML training loops)\n\nWhen NOT to cache:\n- DataFrame is used only once\n- DataFrame is huge and won't fit in memory — thrashing is worse than recomputing\n- Simple, fast transformations\n\nAlways unpersist when done: df.unpersist() — free memory for other operations."
  },
  {
    id: 57, tag: "DATABASES",
    q: "What is database replication and when would a data engineer set it up?",
    a: "Replication copies data from a PRIMARY (source) database to one or more REPLICA (copy) databases in near-real-time.\n\nTypes:\n\nSTREAMING REPLICATION (most common):\n- Replica continuously receives and applies WAL (Write-Ahead Log) changes from primary\n- Replica is a live, up-to-date copy (seconds behind primary)\n- Used for: read replicas to offload query load\n\nLOGICAL REPLICATION:\n- Replicates specific tables or databases (not the full cluster)\n- Can replicate to different Postgres versions or even different databases\n- Used for: CDC pipelines, migrations\n\nWhen a data engineer sets up replication:\n\n1. ANALYTICS QUERIES: never run heavy SELECT analytics on your production OLTP database — it will hurt app performance. Set up a read replica and point your analytics/ETL jobs there.\n\n2. ZERO-DOWNTIME MIGRATIONS: replicate to a new database version or schema, switch over when ready\n\n3. DISASTER RECOVERY: replica in a different region — promote to primary if main region fails\n\n4. CDC SOURCE: Debezium reads from a replica's WAL instead of the primary — no impact on production\n\n5. MULTI-REGION: serve reads from the nearest region for lower latency\n\nRead replica setup in AWS RDS: literally 3 clicks. The hard part is designing your application to use the replica endpoint for reads and primary for writes."
  },
  {
    id: 58, tag: "ETL/ELT",
    q: "What is data deduplication and what strategies do you use?",
    a: "Deduplication is the process of identifying and removing or consolidating duplicate records.\n\nWhy duplicates happen:\n- At-least-once delivery in pipelines: same record ingested twice\n- Retried jobs that didn't check for existing data\n- Merging data from multiple sources that overlap\n- CDC events replayed\n\nSTRATEGIES:\n\n1. DEDUP AT INGESTION — UPSERT (safest):\n   INSERT INTO orders (order_id, revenue, status)\n   VALUES (...)\n   ON CONFLICT (order_id) DO UPDATE SET\n     revenue = EXCLUDED.revenue,\n     status = EXCLUDED.status;\n   -- If order already exists: update it. If not: insert it.\n\n2. DEDUP AT QUERY TIME — ROW_NUMBER():\n   WITH ranked AS (\n     SELECT *,\n       ROW_NUMBER() OVER (PARTITION BY order_id ORDER BY updated_at DESC) AS rn\n     FROM orders\n   )\n   SELECT * FROM ranked WHERE rn = 1;\n   -- Keep only the most recent version of each order_id\n\n3. DEDUP AT WRITE TIME — MERGE (Delta Lake / Snowflake):\n   MERGE INTO orders AS target\n   USING new_orders AS source ON target.order_id = source.order_id\n   WHEN MATCHED THEN UPDATE SET *\n   WHEN NOT MATCHED THEN INSERT *;\n\n4. HASH-BASED DEDUP:\n   Generate a hash of all row values. Only insert rows with unseen hashes.\n   row_hash = md5(order_id || customer_id || revenue || status)\n\nChoose based on volume, latency requirements, and your storage system's capabilities."
  },
  {
    id: 59, tag: "CLOUD",
    q: "What is the difference between a data pipeline running on AWS Glue vs AWS Lambda vs AWS Step Functions?",
    a: "These three serve very different purposes in a data pipeline:\n\nAWS GLUE:\n- Managed Spark/Python ETL service\n- For: processing large datasets (GBs to TBs)\n- Spins up a Spark cluster, runs your job, tears it down\n- You pay per DPU-hour (compute units while job runs)\n- Integrated with Glue Catalog (schema/partition metadata)\n- Best for: batch ETL, large-scale transformations, data catalog management\n\nAWS LAMBDA:\n- Serverless function, runs up to 15 minutes\n- For: lightweight event-driven tasks\n- Triggered by: S3 file upload, API call, schedule (EventBridge), Kafka message\n- Pay per 100ms of execution, scales to zero\n- Max 10GB memory, no persistent storage\n- Best for: trigger a Glue job when a file arrives, send Slack alert, small API calls, lightweight data validation\n\nAWS STEP FUNCTIONS:\n- Orchestration service for chaining AWS services into workflows\n- Defines state machines: task A → if success → task B, if fail → notify → retry\n- Not a compute service itself — it COORDINATES other services\n- Best for: multi-step pipeline orchestration (alternative to Airflow for AWS-native stacks)\n- Example: Step Function that: 1) triggers Lambda to validate file, 2) triggers Glue ETL job, 3) triggers Lambda to notify team\n\nCombined pattern:\n  File lands in S3\n  → Lambda triggered (lightweight check)\n  → Lambda starts Step Function\n  → Step Function runs Glue job\n  → Step Function checks job status\n  → Lambda sends success/failure Slack alert"
  },
  {
    id: 60, tag: "WHY NOT",
    q: "Why normalize data in OLTP but denormalize in OLAP? Aren't they the same thing?",
    a: "This is a key conceptual question. The two systems have OPPOSITE primary concerns.\n\nOLTP (your app's database) — NORMALIZE:\nPrimary concern: write performance, data integrity, avoiding update anomalies\n\nImagine customer 'Alice' lives in 'New York'. If you store city in every order row:\n  order 1: Alice, New York\n  order 2: Alice, New York\n  order 3: Alice, New York\n\nShe moves to LA:\n  UPDATE orders SET city = 'LA' WHERE customer_id = 'C001';\n  -- Must update MILLIONS of rows. Slow. Risky.\n  -- If interrupted midway: some rows say NYC, some say LA. Inconsistent.\n\nNormalized: city stored ONCE in customers table. Update ONE row. Done.\n  UPDATE customers SET city = 'LA' WHERE customer_id = 'C001';\n\nOLAP (data warehouse) — DENORMALIZE:\nPrimary concern: read performance for analytical queries\n\nIf you normalize the warehouse, a simple revenue-by-city query requires:\n  JOIN orders → order_lines → products → product_categories → customers → addresses → cities\n  7 JOINs across billion-row tables = slow and complex\n\nDenormalized star schema:\n  orders_fact already has city, country, product_category pre-joined\n  Simple: SELECT city, SUM(revenue) FROM orders_fact GROUP BY city\n  0 joins. Instant.\n\nThe trade-off:\n- Normalization: storage efficient, write optimized, update safe\n- Denormalization: read optimized, more storage, harder to update\n\nData warehouses rarely UPDATE historical records — they're append-only — so the update anomaly problem doesn't apply."
  },

];